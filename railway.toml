# Railway configuration
[build]
builder = "nixpacks"

[deploy]
# Boot runs catalog hash-sync then serves. See data/catalog_sync.py.
startCommand = "python app.py"
restartPolicyType = "ON_FAILURE"

[env]
PORT = "8793"
